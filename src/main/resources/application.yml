spring:
  application:
    name: userms
  datasource:
    url: jdbc:sqlserver://192.168.3.3:1433;databaseName=userspt;encrypt=true;trustServerCertificate=true;
    username: sa
    password: '040201'
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8082
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
